// services/notasService.js
// CRUD de la tabla `notas` + vínculo opcional a `eventos`.
// TODO (Fase 4): crearNota, listarNotas, vincularNotaAEvento, eliminarNota.
// Usa apiFetch('/notas', ...) de config/apiClient.js -> netlify/functions/notas.js.
