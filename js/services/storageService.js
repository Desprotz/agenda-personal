// services/storageService.js
// Subida de imágenes (notas + agenda) y audio (notas de voz) a Netlify Blobs,
// vía netlify/functions/media.js (el frontend nunca toca el storage directo).
// TODO (Fase 4): subirImagen(file), subirAudio(blob), obtenerUrlPublica(key)
// — usando apiFetch('/media', ...) de config/apiClient.js.
